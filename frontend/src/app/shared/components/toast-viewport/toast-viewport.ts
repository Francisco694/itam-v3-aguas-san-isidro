import { Component, inject } from '@angular/core';
import { LucideCircleCheck, LucideCircleX, LucideInfo, LucideTriangleAlert, LucideX } from '@lucide/angular';
import { ToastService } from '../../../core/services/toast.service';
@Component({selector:'app-toast-viewport',imports:[LucideCircleCheck,LucideCircleX,LucideInfo,LucideTriangleAlert,LucideX],template:`<section class="toasts" aria-live="polite">@for(toast of service.toasts();track toast.id){<article class="toast" [class]="'toast toast--'+toast.tone">@switch(toast.tone){@case('success'){<svg lucideCircleCheck></svg>}@case('error'){<svg lucideCircleX></svg>}@case('warning'){<svg lucideTriangleAlert></svg>}@default{<svg lucideInfo></svg>}}<div><strong>{{toast.title}}</strong>@if(toast.message){<p>{{toast.message}}</p>}</div><button type="button" aria-label="Cerrar notificación" (click)="service.dismiss(toast.id)"><svg lucideX></svg></button></article>}</section>`,styles:[`.toasts{bottom:1.25rem;display:grid;gap:.65rem;max-width:24rem;position:fixed;right:1.25rem;z-index:150}.toast{align-items:flex-start;animation:slide-up .25s ease;background:#fff;border:1px solid var(--gray-200);border-left:4px solid var(--blue);border-radius:.8rem;box-shadow:var(--shadow-md);display:grid;gap:.7rem;grid-template-columns:auto 1fr auto;padding:.85rem}.toast>svg{color:var(--blue);height:1.15rem;width:1.15rem}.toast strong{display:block;font-size:.78rem}.toast p{color:var(--slate-500);font-size:.72rem;margin:.2rem 0 0}.toast button{background:none;border:0;color:var(--slate-400);cursor:pointer;padding:0}.toast button svg{height:.9rem;width:.9rem}.toast--success{border-left-color:var(--success)}.toast--success>svg{color:var(--success)}.toast--error{border-left-color:var(--danger)}.toast--error>svg{color:var(--danger)}.toast--warning{border-left-color:var(--warning)}.toast--warning>svg{color:var(--warning)}@media(max-width:520px){.toasts{bottom:.75rem;left:.75rem;right:.75rem}}`]})
export class ToastViewport { protected readonly service=inject(ToastService); }
