import{Router}from"express";import{requireRole}from"../../shared/auth.middleware";import{actualizarUsuarioController,crearUsuarioController,listarUsuariosController}from"./usuarios.controller";const router=Router();router.use(requireRole("SUPER_USUARIO"));router.get("/",listarUsuariosController);router.post("/",crearUsuarioController);router.patch("/:id",actualizarUsuarioController);export default router;
